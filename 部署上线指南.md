# 网站部署上线指南

## 🚀 推荐方案：Vercel（最简单，免费）

Vercel 是 Next.js 官方推荐的部署平台，提供免费套餐，自动 HTTPS、CDN、全球加速。

### 步骤 1：准备 GitHub 仓库
✅ 已完成 - 代码已推送到：`https://github.com/LoganOYoung/chainsaw-chain-supplier-borealgrit.git`

### 步骤 2：注册 Vercel 账号
1. 访问：https://vercel.com/signup
2. 点击 "Continue with GitHub" 使用 GitHub 账号登录（推荐）

### 步骤 3：部署项目
1. 登录后，点击 "Add New Project"
2. 选择你的仓库：`chainsaw-chain-supplier-borealgrit`
3. 配置项目：
   - **Framework Preset**: Next.js（自动检测）
   - **Root Directory**: `./`（默认）
   - **Build Command**: `npm run build`（默认）
   - **Output Directory**: `.next`（默认）
   - **Install Command**: `npm install`（默认）
4. 点击 "Deploy"

### 步骤 4：等待部署完成
- 通常 2-5 分钟完成
- 部署成功后会自动获得一个 URL，例如：`https://chainsaw-chain-supplier-borealgrit.vercel.app`
- 可以自定义域名（在项目设置中）

### 步骤 5：自动部署
- ✅ 每次推送到 GitHub 的 `main` 分支，Vercel 会自动重新部署
- ✅ 支持预览部署（每个 Pull Request 都会生成预览链接）

---

## 🌐 方案 2：Netlify（同样简单）

### 步骤 1：注册 Netlify
1. 访问：https://app.netlify.com/signup
2. 使用 GitHub 账号登录

### 步骤 2：部署项目
1. 点击 "Add new site" → "Import an existing project"
2. 选择 GitHub，授权后选择仓库
3. 配置构建设置：
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`
   - **Base directory**: `./`（如果项目在子目录）
4. 点击 "Deploy site"

### 步骤 3：完成
- 获得 URL：`https://your-site-name.netlify.app`
- 同样支持自动部署和自定义域名

---

## 🖥️ 方案 3：自托管（需要服务器）

如果你有自己的服务器（VPS、云服务器等），可以自己部署。

### 步骤 1：在服务器上安装 Node.js
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

### 步骤 2：克隆仓库
```bash
git clone https://github.com/LoganOYoung/chainsaw-chain-supplier-borealgrit.git
cd chainsaw-chain-supplier-borealgrit
```

### 步骤 3：安装依赖并构建
```bash
npm install
npm run build
```

### 步骤 4：使用 PM2 运行（推荐）
```bash
# 安装 PM2
npm install -g pm2

# 启动应用
pm2 start npm --name "chainsaw-site" -- start

# 设置开机自启
pm2 startup
pm2 save
```

### 步骤 5：配置 Nginx 反向代理
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 步骤 6：配置 SSL（使用 Let's Encrypt）
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 📋 部署前检查清单

在部署前，确保：

- [x] ✅ 代码已推送到 GitHub
- [ ] 测试本地构建：`npm run build`（确保没有构建错误）
- [ ] 检查 `.gitignore` 已正确配置（排除 `node_modules`、`.next` 等）
- [ ] 确认没有敏感信息（API keys、密码等）硬编码在代码中
- [ ] 检查所有页面是否正常工作

---

## 🔧 本地测试构建

在部署前，建议先本地测试生产构建：

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 访问 http://localhost:3000 测试
```

如果构建成功且网站正常运行，就可以部署了！

---

## 🎯 推荐流程

**最快上线方式（5分钟）：**
1. ✅ 代码已在 GitHub
2. 注册 Vercel 账号（使用 GitHub 登录）
3. 导入 GitHub 仓库
4. 点击 Deploy
5. 完成！获得在线 URL

**后续更新：**
- 直接推送到 GitHub，Vercel 自动部署
- 无需手动操作

---

## 💡 常见问题

### Q: 部署后网站无法访问？
A: 检查构建日志，确保 `npm run build` 成功

### Q: 如何自定义域名？
A: 在 Vercel/Netlify 项目设置中添加自定义域名，然后配置 DNS

### Q: 如何设置环境变量？
A: 在 Vercel/Netlify 项目设置中添加环境变量，无需修改代码

### Q: 部署后样式丢失？
A: 确保 Tailwind CSS 配置正确，检查 `tailwind.config.ts`

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看构建日志
2. 检查 GitHub Actions（如果使用）
3. 查看 Vercel/Netlify 的文档

**推荐：优先使用 Vercel，最简单且免费！**
